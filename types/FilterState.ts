export type FilterState = {
    language?: string
    genre?: string
    duration?: string
    uploadDate?: string
    quality?: string
  }
  
  export type Video = {
    id: string
    title: string
    language: string
    genre: string
    duration: string
    uploadDate: string
    quality: string
  }
  